# 部署到百宝箱完整步骤

## 准备工作

### 1. 创建 npm 账号

访问 https://www.npmjs.com/signup 注册账号

### 2. 修改 package.json 配置

将以下信息替换为你自己的：

```json
{
  "name": "@your-npm-username/ascii-art-mcp-server", // 改为你的npm用户名
  "author": "Your Name", // 改为你的名字
  "repository": {
    "url": "https://github.com/your-github-username/ascii-art-mcp-server.git" // 改为你的仓库
  }
}
```

## 发布到 npm

```bash
# 1. 登录npm
npm login

# 2. 确保构建成功
npm run build

# 3. 测试本地安装
npm pack
npx -y ./your-package-name-1.0.0.tgz

# 4. 发布（如果是scoped package需要public标志）
npm publish --access public

# 5. 确认发布成功
npm view @your-npm-username/ascii-art-mcp-server
```

## 等待 npm 镜像同步

发布后，等待 5-10 分钟让 npmmirror.com 同步你的包。

检查同步状态：

```bash
# 访问 https://npmmirror.com/package/@your-npm-username/ascii-art-mcp-server
```

## 在百宝箱部署

### 1. 进入百宝箱插件市场

访问百宝箱 → 插件 → 新建插件 → 连接 MCP 服务

### 2. 填写部署信息

**基础信息**：

- 插件名称：ASCII Art Generator
- 描述：将图片转换为 ASCII 艺术，支持文本和 PNG 格式输出
- 图标：上传一个合适的图标

**MCP 服务配置**：

- 部署命令：`npx -y @your-npm-username/ascii-art-mcp-server`

### 3. 开始部署

点击"确认"，等待 2-3 分钟自动部署

### 4. 调试工具

部署完成后，会显示两个工具：

- `generate_ascii_art` - 生成 ASCII 文本
- `generate_ascii_image` - 生成 ASCII 图片

逐个测试每个工具，确保都能正常运行。

**测试参数示例**：

```json
{
  "image_path": "d:\\test\\image.jpg",
  "width": 80,
  "charset": "detailed"
}
```

### 5. 发布插件

所有工具调试通过后，点击"发布"按钮。

### 6. 使用插件

在智能体中添加此插件，即可使用 ASCII 艺术生成功能！

## 常见问题

### Q: 部署失败提示"包不存在"

A: 等待 npmmirror.com 同步，或手动访问 https://npmmirror.com/sync/@your-npm-username/ascii-art-mcp-server 触发同步

### Q: 工具调试失败

A: 检查：

1. 图片路径必须是绝对路径
2. 输出路径只能是文件名
3. 百宝箱 Node.js 版本是 22.21.0

### Q: 如何更新版本？

A:

1. 修改 package.json 中的 version
2. npm run build
3. npm publish
4. 在百宝箱重新部署

## npx 命令说明

```bash
npx -y @your-npm-username/ascii-art-mcp-server
```

- `npx`: npm 包执行器
- `-y`: 自动确认安装
- `@your-npm-username/ascii-art-mcp-server`: 你的包名

## 本地测试 npx 命令

在发布前，先本地测试：

```bash
# 构建
npm run build

# 链接到全局
npm link

# 测试npx命令
npx -y ascii-art-mcp-server
```

## 成功标志

✅ npm 发布成功
✅ npmmirror.com 已同步
✅ 百宝箱部署成功
✅ 工具调试通过
✅ 插件发布成功

现在你就可以在百宝箱的智能体中使用 ASCII 艺术生成器了！🎉
